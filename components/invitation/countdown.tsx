"use client";
import { useEffect, useState } from "react";
export function Countdown({ date }: { date: Date }) { const calculate = () => Math.max(0, date.getTime() - Date.now()); const [remaining, setRemaining] = useState(calculate); useEffect(() => { const id = window.setInterval(() => setRemaining(calculate()), 1000); return () => window.clearInterval(id); }, [date]); const days = Math.floor(remaining / 86400000); const hours = Math.floor((remaining % 86400000) / 3600000); const minutes = Math.floor((remaining % 3600000) / 60000); return <div className="mt-6 flex justify-center gap-3">{[[days, "hari"], [hours, "jam"], [minutes, "menit"]].map(([value, label]) => <div className="invite-card min-w-18" key={label as string}><strong className="block text-xl">{value}</strong><span className="text-xs opacity-70">{label as string}</span></div>)}</div>; }
