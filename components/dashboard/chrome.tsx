import Link from "next/link";
import { signOut } from "@/lib/auth";

const items = [["/dashboard", "Ringkasan"], ["/dashboard/events", "Undangan"], ["/dashboard/orders", "Pesanan"], ["/dashboard/profile", "Profil"]];
export function DashboardShell({ children, name, isAdmin }: { children: React.ReactNode; name?: string | null; isAdmin: boolean }) { return <div className="min-h-screen bg-slate-100"><div className="mx-auto flex max-w-7xl flex-col md:flex-row"><aside className="bg-slate-950 p-5 text-white md:min-h-screen md:w-60"><Link className="text-lg font-bold" href="/dashboard">Zenvora <span className="text-indigo-300">Invite</span></Link><p className="mt-2 text-sm text-slate-400">{name ?? "Akun Anda"}</p><nav className="mt-8 flex gap-1 overflow-x-auto md:grid">{items.map(([href, label]) => <Link className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:bg-white/10" href={href} key={href}>{label}</Link>)}{isAdmin && <Link className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:bg-white/10" href="/admin">Admin panel</Link>}</nav><form className="mt-8" action={async () => { "use server"; await signOut({ redirectTo: "/" }); }}><button className="text-sm text-slate-400 hover:text-white">Keluar</button></form></aside><section className="min-w-0 flex-1 p-5 sm:p-8">{children}</section></div></div>; }
export function EventTabs({ eventId }: { eventId: string }) { const tabs = [["", "Ringkasan"], ["/content", "Konten"], ["/design", "Desain"], ["/guests", "Tamu"], ["/rsvp", "RSVP"], ["/wishes", "Ucapan"], ["/gifts", "Kado"], ["/checkin", "Check-in"], ["/analytics", "Analitik"]]; return <nav className="mb-6 flex gap-2 overflow-x-auto border-b border-slate-200 pb-3">{tabs.map(([suffix, label]) => <Link className="whitespace-nowrap rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-white hover:text-indigo-700" href={`/dashboard/events/${eventId}${suffix}`} key={suffix}>{label}</Link>)}</nav>; }
