import { SectionHeading } from "@/components/section-heading";
export default function FeaturesPage() { const items = ["Katalog delapan tema orisinal", "Wizard acara dengan simpan otomatis", "Tautan personal dan QR tamu", "RSVP dengan batas kursi", "Ucapan termoderasi", "Check-in seluler", "Pembayaran mock yang siap diganti adapter", "Analitik yang berfokus pada acara"]; return <main className="shell py-14"><SectionHeading eyebrow="Fitur" title="Seluruh alur undangan, lebih ringkas" body="Dibuat untuk pemilik acara dan panitia yang ingin fokus pada tamu, bukan pada kerumitan teknis." /><div className="mt-8 grid gap-3 sm:grid-cols-2">{items.map((item, index) => <div className="card flex gap-4 p-5" key={item}><span className="grid size-7 shrink-0 place-items-center rounded-full bg-indigo-100 text-sm font-bold text-indigo-700">{index + 1}</span><p className="font-semibold">{item}</p></div>)}</div></main>; }
