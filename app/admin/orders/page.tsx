import { prisma } from "@/lib/prisma";
import { formatCurrency, formatDate } from "@/lib/utils";
export const dynamic = "force-dynamic";
export default async function AdminOrders() { const orders = await prisma.order.findMany({ include: { user: true, event: true, payments: true }, orderBy: { createdAt: "desc" }, take: 100 }); return <><h2 className="text-2xl font-bold">Pesanan</h2><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-150 text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-3">Kode</th><th className="p-3">Pelanggan</th><th className="p-3">Total</th><th className="p-3">Status</th><th className="p-3">Waktu</th></tr></thead><tbody>{orders.map(order => <tr className="border-b" key={order.id}><td className="p-3 font-semibold">{order.code}</td><td className="p-3">{order.user.email}</td><td className="p-3">{formatCurrency(order.total, order.currency)}</td><td className="p-3">{order.status}</td><td className="p-3">{formatDate(order.createdAt)}</td></tr>)}</tbody></table></div></>; }
