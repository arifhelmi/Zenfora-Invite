import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AdminUsers() { const users = await prisma.user.findMany({ include: { role: true, _count: { select: { events: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <><h2 className="text-2xl font-bold">Pengguna</h2><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-150 text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-3">Nama</th><th className="p-3">Email</th><th className="p-3">Peran</th><th className="p-3">Acara</th></tr></thead><tbody>{users.map(user => <tr className="border-b" key={user.id}><td className="p-3 font-semibold">{user.name ?? "—"}</td><td className="p-3">{user.email}</td><td className="p-3">{user.role.key}</td><td className="p-3">{user._count.events}</td></tr>)}</tbody></table></div></>; }
