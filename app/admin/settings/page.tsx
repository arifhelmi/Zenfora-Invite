import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AdminSettings() { const settings = await prisma.systemSetting.findMany({ orderBy: { key: "asc" } }); return <><h2 className="text-2xl font-bold">Konfigurasi sistem</h2><p className="mt-2 text-slate-600">Nilai operasional dapat dipindahkan ke environment variable atau dikelola di sini. Rahasia tidak pernah ditampilkan.</p><div className="mt-5 grid gap-3">{settings.map(setting => <div className="card p-5" key={setting.id}><h3 className="font-mono text-sm font-bold">{setting.key}</h3><pre className="mt-2 overflow-x-auto rounded bg-slate-50 p-3 text-xs text-slate-600">{JSON.stringify(setting.value, null, 2)}</pre></div>)}{settings.length === 0 && <div className="card p-5 text-slate-600">Belum ada konfigurasi tersimpan.</div>}</div></>; }
