import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AdminPage() { const [users, events, orders, revenue] = await Promise.all([prisma.user.count({ where: { deletedAt: null } }), prisma.event.count({ where: { deletedAt: null } }), prisma.order.count(), prisma.order.aggregate({ where: { status: "PAID" }, _sum: { total: true } })]); return <><h2 className="text-2xl font-bold">Ringkasan platform</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Pengguna", users], ["Undangan", events], ["Pesanan", orders], ["Pendapatan tervalidasi", new Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", maximumFractionDigits: 0 }).format(revenue._sum.total ?? 0)]].map(([label, value]) => <div className="card p-5" key={label as string}><p className="text-sm text-slate-500">{label as string}</p><p className="mt-2 text-2xl font-bold">{value as string | number}</p></div>)}</div></>; }
