import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AdminReports() { const byStatus = await prisma.event.groupBy({ by: ["status"], _count: { _all: true } }); const visits = await prisma.invitationVisit.count(); return <><h2 className="text-2xl font-bold">Laporan platform</h2><p className="mt-2 text-slate-600">Ringkasan agregat untuk pemantauan operasional.</p><div className="mt-5 grid gap-4 sm:grid-cols-3"><div className="card p-5"><p className="text-sm text-slate-500">Kunjungan tercatat</p><p className="mt-1 text-3xl font-bold">{visits}</p></div>{byStatus.map(item => <div className="card p-5" key={item.status}><p className="text-sm text-slate-500">Undangan {item.status}</p><p className="mt-1 text-3xl font-bold">{item._count._all}</p></div>)}</div></>; }
