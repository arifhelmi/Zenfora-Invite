import { prisma } from "@/lib/prisma";
import { formatCurrency, formatDate } from "@/lib/utils";
export const dynamic = "force-dynamic";
export default async function AdminPayments() { const payments = await prisma.payment.findMany({ include: { order: { include: { user: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <><h2 className="text-2xl font-bold">Pembayaran</h2><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-150 text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-3">External ID</th><th className="p-3">Pelanggan</th><th className="p-3">Jumlah</th><th className="p-3">Status</th><th className="p-3">Waktu</th></tr></thead><tbody>{payments.map(payment => <tr className="border-b" key={payment.id}><td className="p-3 font-mono text-xs">{payment.externalId}</td><td className="p-3">{payment.order.user.email}</td><td className="p-3">{formatCurrency(payment.amount)}</td><td className="p-3">{payment.status}</td><td className="p-3">{formatDate(payment.createdAt)}</td></tr>)}</tbody></table></div></>; }
