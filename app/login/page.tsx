import { LoginForm } from "@/components/auth-forms";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ registered?: string }> }) { const params = await searchParams; return <main className="shell grid min-h-[70vh] place-items-center py-12"><section className="card w-full max-w-md p-7"><p className="eyebrow">Selamat datang kembali</p><h1 className="mt-2 text-3xl font-bold">Masuk ke Zenvora</h1>{params.registered && <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Akun dibuat. Silakan masuk.</p>}<LoginForm /></section></main>; }
