import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function GiftsPage({ params }: { params: Promise<{ eventId: string }> }) { const { eventId } = await params; const gifts = await prisma.giftMethod.findMany({ where: { eventId }, orderBy: { createdAt: "desc" } }); return <><p className="eyebrow">Kado digital</p><h1 className="mt-1 text-3xl font-bold">Pilihan pemberian untuk tamu</h1><p className="mt-2 text-slate-600">Tambahkan rekening, e-wallet, atau registry dari pengaturan acara sebelum dipublikasikan.</p><div className="mt-6 grid gap-3">{gifts.map(gift => <div className="card p-5" key={gift.id}><strong>{gift.label}</strong><p className="mt-1 text-sm text-slate-600">{gift.type} · {gift.account ?? gift.url ?? "Belum ada detail"}</p></div>)}{gifts.length === 0 && <div className="card p-5 text-slate-600">Belum ada metode kado untuk undangan ini.</div>}</div></>; }
