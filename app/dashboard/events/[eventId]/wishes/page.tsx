import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function WishesPage({ params }: { params: Promise<{ eventId: string }> }) { const { eventId } = await params; const wishes = await prisma.wish.findMany({ where: { eventId }, orderBy: { createdAt: "desc" } }); return <><p className="eyebrow">Ucapan</p><h1 className="mt-1 text-3xl font-bold">Pesan dari para tamu</h1><div className="mt-7 grid gap-3">{wishes.map(wish => <article className="card p-5" key={wish.id}><div className="flex justify-between gap-4"><h2 className="font-bold">{wish.author}</h2><span className="status">{wish.status}</span></div><p className="mt-2 text-slate-600">{wish.message}</p></article>)}{wishes.length === 0 && <p className="card p-5 text-slate-600">Belum ada ucapan.</p>}</div></>; }
