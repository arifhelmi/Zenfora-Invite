import { requireUser } from "@/lib/permissions";
export default async function ProfilePage() { const user = await requireUser(); return <><p className="eyebrow">Profil</p><h1 className="mt-1 text-3xl font-bold">Akun Anda</h1><div className="card mt-7 max-w-xl p-6"><dl className="grid gap-4 text-sm"><div><dt className="text-slate-500">Nama</dt><dd className="mt-1 font-semibold">{user.name ?? "—"}</dd></div><div><dt className="text-slate-500">Email</dt><dd className="mt-1 font-semibold">{user.email}</dd></div><div><dt className="text-slate-500">Peran</dt><dd className="mt-1 font-semibold">{user.role}</dd></div></dl><button className="button danger mt-7" disabled>Hapus akun (memerlukan konfirmasi tambahan)</button></div></>; }
