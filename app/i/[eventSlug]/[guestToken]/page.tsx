import { notFound } from "next/navigation";
import QRCode from "qrcode";
import { prisma } from "@/lib/prisma";
import { InvitationRenderer } from "@/components/invitation/renderer";
export const dynamic = "force-dynamic";
export default async function GuestInvitationPage({ params }: { params: Promise<{ eventSlug: string; guestToken: string }> }) { const { eventSlug, guestToken } = await params; const event = await prisma.event.findFirst({ where: { slug: eventSlug, status: "PUBLISHED" }, include: { theme: { include: { versions: true } }, people: { orderBy: { position: "asc" } }, schedules: { orderBy: { position: "asc" } }, sections: { orderBy: { position: "asc" } }, wishes: { where: { status: "APPROVED" }, select: { id: true, author: true, message: true }, take: 20 } } }); const guest = event && await prisma.guest.findFirst({ where: { eventId: event.id, token: guestToken, deletedAt: null } }); if (!event || !event.theme || !guest) notFound(); await prisma.invitationVisit.create({ data: { eventId: event.id, guestId: guest.id, day: new Date() } }); const qr = await QRCode.toDataURL(guestToken, { width: 180, margin: 1 }); return <><InvitationRenderer theme={event.theme} event={event} guest={{ token: guest.token, name: guest.name, seats: guest.seats }} /><div className="fixed bottom-4 right-4 rounded-xl bg-white p-2 shadow-lg"><img src={qr} width="80" height="80" alt="QR token tamu" /><span className="visually-hidden">QR token untuk check-in</span></div></>; }
